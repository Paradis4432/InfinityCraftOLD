package com.infinitycraft.plugin.essentialCommands;

public class UpVoteCommand {
    /**
     * A simple /upvote command
     * @param sender The sender of the command
     * @param cmd The command
     * @param label The label of the command
     * @param args The commands arguments
     * @return Whether or not the command was used successfully
     * upvotes a player
     * open sql search for player and table, set upvotes += 1
     * set how many upvotes we would need to flag a player as a good player and rank him up to the next level (helper, helper manager, mod, etc) , my idea is ( 50, 150, 300, 500)
     * a player can only upvote the same person once
     */
}
